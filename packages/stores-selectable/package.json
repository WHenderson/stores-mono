{
  "name": "@crikey/stores-selectable",
  "version": "1.0.0",
  "description": "",
  "main": "src/index",
  "files": [
    "dist"
  ],
  "typedocMain": "src/index.ts",
  "publishConfig": {
    "main": "dist/index",
    "module": "dist/index.mjs",
    "types": "dist/index.d.ts"
  },
  "scripts": {
    "build": "tsc && vite build && rollup --config rollup.config.ts --configPlugin typescript",
    "clean": "rimraf dist",
    "test": "vitest run",
    "debug": "vitest run --no-threads --no-isolate",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "coverage:watch": "vitest --coverage"
  },
  "dependencies": {
    "@crikey/stores-base": "workspace:^1.0.0"
  },
  "devDependencies": {
    "@crikey/stores-immer": "workspace:^1.0.0",
    "@crikey/stores-strict": "workspace:^1.0.0",
    "@crikey/stores-svelte": "workspace:^1.0.0"
  }
}
