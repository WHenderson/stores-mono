{
  "name": "@crikey/stores-base-queue",
  "version": "1.0.0",
  "description": "",
  "main": "src/index",
  "files": [
    "dist"
  ],
  "typedocMain": "src/index.ts",
  "publishConfig": {
    "main": "dist/index",
    "module": "dist/index.mjs",
    "types": "dist/index.d.ts"
  },
  "scripts": {
    "build": "tsc && vite build && rollup --config rollup.config.ts --configPlugin typescript",
    "build:docs:watch": "pnpm exec typedoc --watch --out dist/docs src/index.ts",
    "clean": "rimraf dist",
    "test": "vitest run",
    "debug": "vitest run --no-threads --no-isolate",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "coverage:watch": "vitest --coverage"
  }
}
