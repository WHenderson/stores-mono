{
  "name": "@crikey/stores-immer",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && vite build && rollup --config rollup.config.ts --configPlugin typescript",
    "test": "vitest run",
    "debug": "vitest run --no-threads --no-isolate",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "coverage:watch": "vitest --coverage"
  },
  "dependencies": {
    "@crikey/stores-base": "workspace:^1.0.0",
    "@crikey/stores-strict": "workspace:^1.0.0"
  },
  "devDependencies": {
    "immer": "^9.0.12"
  },
  "peerDependencies": {
    "immer": "^9.0.12"
  }
}
